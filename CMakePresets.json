{
    "version": 2,
    "configurePresets": [
        {
            "name": "release",
            "displayName": "Release (AT90CAN128)",
            "description": "Release build for AT90CAN128 hardware.",
            "generator": "Ninja",
            "binaryDir": "${sourceDir}/bin/release",
            "cacheVariables": {
                "CMAKE_BUILD_TYPE": "Release"
            }
        }
    ],
    "buildPresets": [
        {
            "name": "release",
            "configurePreset": "release",
            "description": "Build release AT90CAN128 target"
        }
    ]
}